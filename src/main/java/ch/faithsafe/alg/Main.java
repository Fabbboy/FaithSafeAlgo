package ch.faithsafe.alg;

public class Main {
  public static void main(String[] args) {
    FaithSafeEncryption encryption = new FaithSafeEncryption();
    /*
    // Original data to encrypt
    String originalData = "Hello, World!";
    byte[] encryptedData = encryption.encrypt(originalData.getBytes());

    // Decrypt data
    byte[] decryptedData = encryption.decrypt(encryptedData);

    // Additional test to ensure that public key is consistent
    byte[] publicKey = encryption.getPublicKey();

    System.out.println("Public key: " + Arrays.toString(publicKey));
    System.out.println("Original data: " + originalData);
    System.out.println("Encrypted data: " + Arrays.toString(encryptedData));
    System.out.println("Decrypted data: " + new String(decryptedData));
 */
  }
}